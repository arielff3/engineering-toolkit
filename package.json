{
  "name": "engineering-toolkit",
  "private": true,
  "version": "0.1.0",
  "description": "Engineering decisions as code",
  "scripts": {
    "build": "pnpm -r run build",
    "clean": "pnpm -r run clean"
  },
  "engines": {
    "node": ">=18",
    "pnpm": ">=9"
  },
  "packageManager": "pnpm@9.15.0",
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
